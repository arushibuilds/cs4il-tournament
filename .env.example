# Absolute base URL of the deployed site. Used to resolve Open Graph / Twitter
# image URLs at build time. Falls back to http://localhost:3000 when unset.
NEXT_PUBLIC_SITE_URL=https://example.org

# Add further NEXT_PUBLIC_* entries here as new content needs them.
