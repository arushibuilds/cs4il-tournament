import type { Metadata } from 'next';
import ComingSoon from '@/components/ComingSoon';

export const metadata: Metadata = {
  title: 'Tournament | CS4IL',
};

export default function Tournament() {
  return <ComingSoon />;
}
